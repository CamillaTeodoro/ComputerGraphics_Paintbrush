export class Circumference {
  constructor(center, radius, color) {
    this.center = center;
    this.radius = radius;
    this.color = color;
  }

  setRadius(radius) {
    this.radius = radius;
  }
}
